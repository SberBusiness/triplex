/** Вариант выбора даты. */
export enum EPickType {
    datePick,
    monthYearPick,
}

/**
 * Направление переключения страницы календаря.
 */
export enum EChangePageDirection {
    FORWARD,
    BACKWARD,
}
