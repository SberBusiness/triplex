/** Темы кнопки. */
export enum EButtonTheme {
    GENERAL = 'general',
    SECONDARY = 'secondary',
    DANGER = 'danger',
    DOTS = 'dots',
    LINK = 'link',
    TILE = 'tile',
}

/** Размеры кнопки. */
export enum EButtonSize {
    LG = 'LG',
    MD = 'MD',
    SM = 'SM',
}

/** Форма границы кнопки. */
export enum EButtonIconShape {
    SQUIRCLE = 'squircle',
    CIRCLE = 'circle',
}
