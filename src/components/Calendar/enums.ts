/** Вариант выбора даты. */
export enum ECalendarPickType {
    datePick,
    monthYearPick,
}

/** Режим отображения календаря. */
export enum ECalendarViewMode {
    DAYS = 'days',
    MONTHS = 'months',
    YEARS = 'years',
}
