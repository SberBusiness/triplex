# This is a basic workflow to help you get started with Actions

name: Styleguidist deploy

# Controls when the workflow will run
on:

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Run a multi-line script
        run: |
          if [ "${{ secrets.TRIPLEX_DEV_IP }}" -eq "188.120.224.128" ]
          then
            echo TRIPLEX_DEV_IP is working.
          fi
          echo test, and deploy your project.
      - name: executing remote ssh commands using password
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.TRIPLEX_DEV_IP }}
          username: ${{ secrets.TRIPLEX_DEV_SSH_USER }}
          password: ${{ secrets.TRIPLEX_DEV_SSH_USER_PASS }}
          script: whoami
